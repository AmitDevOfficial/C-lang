#include<stdio.h>

int main(){
    int a = 2;
    int b = 2;
    int c = a + b;

    printf("The value of a is %d and the value of b is %d and the sum is %d\n", a, b, c);
    //Modules operator to get the remainder
    printf("The remider when a divided by b is: %d\n", a%b);
    return 0;
}