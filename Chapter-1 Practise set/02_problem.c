#include<stdio.h>

int main(){
    int radius = 6;

    printf("The area of circle with radius %d is %f\n", radius, 3.14*radius*radius);

    int height = 10;
    printf("The Volume of cylinder is %d and height %d is %f", radius, height, 3.14*radius*radius*height);
    return 0;
}